<?xml version="1.0" encoding="utf-8"?>
<!-- res/layout/item_category_tab.xml -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_marginEnd="16dp">

    <FrameLayout
        android:id="@+id/flCategoryBg"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:background="@drawable/bg_category_normal">

        <!-- Icon placeholder: dùng first letter hoặc thay bằng ImageView nếu có icon -->
        <TextView
            android:id="@+id/tvCategoryIcon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="🧋"
            android:textSize="22sp" />

    </FrameLayout>

    <TextView
        android:id="@+id/tvCategoryName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="11sp"
        android:textColor="@color/milktea_text_secondary"
        android:maxLines="1"
        android:ellipsize="end"
        android:maxWidth="64dp" />

</LinearLayout>